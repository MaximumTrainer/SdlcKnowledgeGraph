// The default VitePress theme with the project's colours. No layout is overridden and no page is
// written here; the content still comes entirely from the generated pages under src/.
import DefaultTheme from 'vitepress/theme'
import './custom.css'

export default DefaultTheme

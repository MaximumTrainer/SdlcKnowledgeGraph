cucumber.publish.quiet=true
cucumber.plugin.names.enabled=true
# Features are discovered by RunCucumberTest (@Suite); do not let Gradle's classpath scan run them a second time.
cucumber.junit-platform.discovery.as-root-engine=false
